# 마이페이지 모바일 레이아웃 수정 작업 내역

## 📅 작업 정보
- **작업일시**: 2026-01-03
- **작업 대상**: 마이페이지 모바일 레이아웃 개선
- **작업자**: Claude Code

---

## 🎯 작업 목적

모바일 환경에서 발생하는 다음 두 가지 문제 해결:
1. **버튼 레이아웃 깨짐**: "추천인공유", "정보수정", "계정삭제" 버튼들이 줄바꿈되며 UI가 깨지는 현상
2. **상단 콘텐츠 가려짐**: 페이지 상단이 헤더에 가려져 위로 올라가 보이는 현상

---

## 🔧 수정 내용

### 1. 파일 백업

**백업 파일**:
- [home.skin.php.backup](f:/0push_project_www/hairwang/www/theme/rb.basic/skin/member/rb.member/home.skin.php.backup)
- [style.css.backup](f:/0push_project_www/hairwang/www/theme/rb.basic/skin/member/rb.member/style.css.backup)

**복구 방법** (필요시):
```bash
# home.skin.php 복구
cp f:/0push_project_www/hairwang/www/theme/rb.basic/skin/member/rb.member/home.skin.php.backup f:/0push_project_www/hairwang/www/theme/rb.basic/skin/member/rb.member/home.skin.php

# style.css 복구
cp f:/0push_project_www/hairwang/www/theme/rb.basic/skin/member/rb.member/style.css.backup f:/0push_project_www/hairwang/www/theme/rb.basic/skin/member/rb.member/style.css
```

---

### 2. home.skin.php 수정

**파일**: [home.skin.php](f:/0push_project_www/hairwang/www/theme/rb.basic/skin/member/rb.member/home.skin.php)

**수정 위치**: Line 391-452 (미디어 쿼리 섹션)

**추가된 CSS**:
```css
@media (max-width: 768px) {
    /* 기존 코드 유지 */

    /* 버튼 영역 모바일 최적화 */
    .rb_prof .copy_urls li {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        align-items: center;
        justify-content: flex-start;
    }

    .rb_prof .copy_urls {
        height: auto !important;
        padding: 10px;
        line-height: normal;
    }

    /* 모든 버튼 크기 통일 */
    .rb_prof_btn .fl_btns,
    .rb_prof_btn .fl_btns_txt,
    .referral-share-trigger-btn {
        height: 32px !important;
        line-height: 32px !important;
        padding: 0 12px !important;
        font-size: 12px !important;
        white-space: nowrap;
        margin: 0 !important;
    }

    /* 아이콘 버튼 */
    .rb_prof_btn .fl_btns img,
    .rb_prof_btn .fl_btns svg {
        width: 20px !important;
        height: 20px !important;
    }

    /* 추천인 버튼 SVG */
    .referral-share-trigger-btn svg {
        width: 14px !important;
        height: 14px !important;
    }
}
```

**수정 효과**:
- ✅ 버튼들이 자동으로 줄바꿈되며 정렬됨 (flex-wrap)
- ✅ 모든 버튼의 높이가 32px로 통일
- ✅ 버튼 간격이 8px로 균등하게 배치
- ✅ 작은 화면에서도 가독성 유지 (font-size: 12px)
- ✅ 아이콘 크기 통일로 시각적 일관성 확보

---

### 3. style.css 수정

**파일**: [style.css](f:/0push_project_www/hairwang/www/theme/rb.basic/skin/member/rb.member/style.css)

#### 3-1. 기본 상단 여백 추가

**수정 위치**: Line 406

**변경 전**:
```css
.rb_prof {margin-top: 0px; position: relative;}
```

**변경 후**:
```css
.rb_prof {margin-top: 20px; position: relative;}
```

**수정 효과**:
- ✅ 데스크톱에서 상단에 20px 여백 추가

---

#### 3-2. 모바일 상단 여백 강화

**수정 위치**: Line 477-490 (모바일 미디어 쿼리)

**변경 전**:
```css
@media all and (max-width:1024px) {
    .rb_prof {padding-left: 20px; padding-right: 20px;}
    /* ... */
}
```

**변경 후**:
```css
@media all and (max-width:1024px) {
    .rb_prof {padding-left: 20px; padding-right: 20px; margin-top: 80px; padding-top: 20px;}
    /* ... */
}
```

**수정 효과**:
- ✅ 모바일/태블릿에서 상단에 80px 여백 추가 (헤더 높이 고려)
- ✅ 추가로 20px 패딩으로 안전 여백 확보
- ✅ 고정 헤더에 의해 콘텐츠가 가려지는 현상 방지

---

## 📊 수정 전후 비교

### 버튼 레이아웃

| 항목 | 수정 전 | 수정 후 |
|------|---------|---------|
| 버튼 배치 | 한 줄에 5개 강제 배치 | 화면 크기에 따라 자동 줄바꿈 |
| 버튼 높이 | 불균일 (32px, 35px) | 통일 (32px) |
| 버튼 간격 | 불규칙 | 균등 (8px gap) |
| 가독성 | ❌ 깨짐 발생 | ✅ 정상 표시 |
| 터치 영역 | ❌ 불편 | ✅ 개선 |

### 상단 여백

| 디바이스 | 수정 전 | 수정 후 |
|----------|---------|---------|
| 데스크톱 | 0px | 20px |
| 태블릿/모바일 (1024px 이하) | 0px | 80px + 20px padding |
| 결과 | ❌ 헤더에 가려짐 | ✅ 정상 표시 |

---

## 🧪 테스트 가이드

### 1. 브라우저 개발자 도구 테스트

```
1. Chrome 개발자 도구 열기 (F12)
2. 디바이스 툴바 활성화 (Ctrl+Shift+M)
3. 다양한 화면 크기 테스트:
   - iPhone SE (375px)
   - iPhone 12 Pro (390px)
   - iPhone 14 Pro Max (430px)
   - iPad (768px)
   - iPad Pro (1024px)
```

### 2. 실제 디바이스 테스트

**iOS**:
- Safari 모바일
- Chrome 모바일

**Android**:
- Chrome 모바일
- Samsung Internet

### 3. 확인 사항

#### 버튼 레이아웃
- [ ] 버튼들이 정상적으로 표시되는가?
- [ ] 버튼 높이가 동일한가?
- [ ] 버튼 간격이 균등한가?
- [ ] 긴 텍스트가 있어도 깨지지 않는가?
- [ ] 터치하기 편한가?

#### 상단 여백
- [ ] 프로필 사진이 헤더에 가려지지 않는가?
- [ ] 닉네임과 레벨 뱃지가 정상 표시되는가?
- [ ] 스크롤이 자연스러운가?
- [ ] 아이폰 노치/다이나믹 아일랜드에 가려지지 않는가?

---

## 🔍 문제 발생 시 확인 사항

### 1. 버튼이 여전히 깨진다면

**확인 1**: 캐시 삭제
```
모바일 브라우저 설정 → 방문 기록 삭제 → 캐시/쿠키 삭제
```

**확인 2**: 버전 쿼리스트링 확인
[home.skin.php:5](f:/0push_project_www/hairwang/www/theme/rb.basic/skin/member/rb.member/home.skin.php#L5)
```php
add_stylesheet('<link rel="stylesheet" href="'.$member_skin_url.'/style.css?ver='.G5_TIME_YMDHIS.'">', 0);
```
→ `G5_TIME_YMDHIS` 상수가 자동으로 캐시를 무효화합니다.

**확인 3**: CSS 우선순위
- `!important`를 사용하여 다른 CSS를 오버라이드했는지 확인
- 브라우저 개발자 도구에서 실제 적용된 CSS 확인

### 2. 상단 여백이 부족하거나 과하다면

**헤더 높이 측정**:
```javascript
// 브라우저 콘솔에서 실행
document.querySelector('header').offsetHeight
```

**여백 조정**:
[style.css:478](f:/0push_project_www/hairwang/www/theme/rb.basic/skin/member/rb.member/style.css#L478)에서 `margin-top` 값을 조정
```css
.rb_prof {margin-top: 헤더높이px; ...}
```

### 3. iOS 노치 대응 필요 시

HTML `<head>` 태그에 다음 추가:
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
```

CSS에 Safe Area 추가:
```css
@media (max-width: 1024px) {
    .rb_prof {
        margin-top: max(80px, env(safe-area-inset-top));
        padding-top: 20px;
    }
}
```

---

## 📁 수정 파일 목록

1. **home.skin.php** - 버튼 레이아웃 개선
   - 위치: `f:/0push_project_www/hairwang/www/theme/rb.basic/skin/member/rb.member/home.skin.php`
   - 백업: `home.skin.php.backup`

2. **style.css** - 상단 여백 추가
   - 위치: `f:/0push_project_www/hairwang/www/theme/rb.basic/skin/member/rb.member/style.css`
   - 백업: `style.css.backup`

---

## ✅ 완료 체크리스트

- [x] 원본 파일 백업 생성
- [x] home.skin.php 모바일 CSS 수정
- [x] style.css 상단 여백 추가
- [x] 작업 내역 문서화
- [ ] 모바일 브라우저 테스트 (사용자 수행 필요)
- [ ] 실제 디바이스 테스트 (사용자 수행 필요)
- [ ] 다양한 화면 크기 확인 (사용자 수행 필요)

---

## 💡 추가 개선 제안

### 1. 반응형 폰트 크기
현재 고정 크기(12px)보다 반응형 단위 사용 권장:
```css
font-size: clamp(11px, 2.5vw, 14px);
```

### 2. 버튼 우선순위 강조
중요한 버튼(정보수정, 추천인공유)을 시각적으로 강조:
```css
.referral-share-trigger-btn {
    background: linear-gradient(135deg, #7a4efe 0%, #6840e0 100%);
    box-shadow: 0 2px 8px rgba(122, 78, 254, 0.3);
}
```

### 3. 다크모드 대응
다크모드 환경에서의 가독성 개선:
```css
@media (prefers-color-scheme: dark) {
    .rb_prof_btn .fl_btns {
        background: #2d2d2d;
        color: #fff;
    }
}
```

---

## 📞 문의 및 지원

문제 발생 시:
1. 브라우저 캐시 삭제 후 재테스트
2. 백업 파일로 복구 후 재시도
3. 작업 내역 참고하여 단계별 확인

---

**작업 완료**: 2026-01-03
